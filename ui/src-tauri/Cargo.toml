[package]
name = "instinct-muse"
version = "0.1.1"
description = "Instinct Muse - open-source personal AI agent"
edition = "2021"

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri-plugin-shell = "2"

[build-dependencies]
tauri-build = { version = "2", features = [] }
